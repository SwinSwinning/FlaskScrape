from app import app
# from app.models import User, Game, exGame

# @app.shell_context_processor
# def make_shell_context():
#     return {User: "User", Game: "Game", exGame: "exGame", db: "db"} 


if __name__ == "__main__":
    # settings = get_project_settings()
    # settings['FEEDS'] = {'from_main.json': {'format': 'json', 'overwrite': 'true'}}

    # process = CrawlerProcess(settings)
    # process.crawl(PSNScraper)
    # process.start()




    print("done-----------------------------------------------")
